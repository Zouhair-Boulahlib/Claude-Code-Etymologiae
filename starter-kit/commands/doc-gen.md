---
description: Generate or update documentation for changed code
---

Look at recent changes (`git diff main...HEAD` or `git diff HEAD~3`) and:

1. Update any affected README sections
2. Add or update JSDoc/docstrings for new or changed public functions
3. Update API documentation if endpoints changed
4. Flag any documentation that references code that no longer exists

Write the documentation updates directly. Keep docs concise and accurate.

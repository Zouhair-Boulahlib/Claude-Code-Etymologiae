{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "which notify-send >/dev/null 2>&1 && notify-send 'Claude Code' 'Task completed' || which osascript >/dev/null 2>&1 && osascript -e 'display notification \"Task completed\" with title \"Claude Code\"' || true"
          }
        ]
      }
    ]
  }
}

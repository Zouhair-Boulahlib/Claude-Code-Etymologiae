{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'git commit'; then npm test 2>&1 || echo 'BLOCK: Tests failed. Fix before committing.'; fi"
          }
        ]
      }
    ]
  }
}
